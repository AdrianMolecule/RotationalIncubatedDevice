┌────────────────────┐
           │ Browser UI      │
           │-- -- -- -- -- -- -- -- -- --│
           │ Editable table :     │
           │ ID | Name | Type |  │
           │ Value | Description│
           └─────────┬──────────┘
                     │ WebSocket send│ "field=value" on change
                     │
                     ▼
           ┌────────────────────┐
           │ Web         │
           │-- -- -- -- -- -- -- -- -- --│
           │ AsyncWebServer     │
           │ AsyncWebSocket     │
           │ onMessage()        │
           │ notifyClients() → WebSocket broadcast
           └─────────┬──────────┘
                     │ Updates Model fields
                     │
                     ▼
           ┌────────────────────┐
           │ Model        │
           │-- -- -- -- -- -- -- -- -- --│
           │ Holds Fields[]     │
           │ getValue() / setValue() │
           └─────────┬──────────┘
                     │ Serial input │ "f1=42" updates model
                     ▼
           ┌────────────────────┐
           │ Serial       │
           │-- -- -- -- -- -- -- -- -- --│
           │ User types updates │
           │ into ESP32 Serial  │
           └────────────────────┘
